using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MazeBoundsCheck : MonoBehaviour
{
    //Placeholder class for piece-based trigger checks
    //May be used as a means of only loading in specific areas of the maze based on if the player is within the bounds

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
